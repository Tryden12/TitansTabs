<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.tryden12.titanstabs" >

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label=""
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.TitansTabs" >
        <activity
            android:name=".ui.main.view.activities.MainActivity"
            android:exported="true" />
        <activity
            android:name=".ui.main.view.activities.LoginActivity"
            android:exported="true"
            android:theme="@style/LoginCreate"/>
        <activity
            android:name=".ui.main.view.activities.CreateUserActivity"
            android:exported="true"
            android:theme="@style/LoginCreate"/>

        <activity
            android:name=".ui.main.view.splashscreen.SplashScreen"
            android:exported="true"
            android:theme="@style/SplashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>